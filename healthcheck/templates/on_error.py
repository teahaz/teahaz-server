def on_error(message):
    """
        Users can add code here that gets executed if the healthcheck fails

        Examples could be some sort of notification, or direct-message sent with some social media api
    """

    return
